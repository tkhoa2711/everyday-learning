# Display database query results vertically

PostgresQL:

```
psql> \x
Expanded display is on.
```

or by supplying `psql` with `-x` flag.

MySQL:

```
mysql> show databases\G
```

Or starting `mysql` with `--vertical` option.
